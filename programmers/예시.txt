# 프로그래머스 Lv1. 최댓값과 최솟값

## 문제 설명
주어진 문자열에서 공백으로 구분된 숫자들 중 최댓값과 최솟값을 찾아 문자열로 반환하는 문제입니다.

## 입력 예시
"1 2 3 4" → "4 1"  
"-1 -2 -3 -4" → "-1 -4"

## 코드
```js
function solution(s) {
  const arr = s.split(' ').map(Number);
  return `${Math.max(...arr)} ${Math.min(...arr)}`;
}

## 풀이 설명

## 배운 점